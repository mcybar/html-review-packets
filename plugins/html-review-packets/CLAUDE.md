# HTML Review Packets

Use the `html-review-packets` skill when the user wants generated HTML to support inline review comments, or when they provide a submitted review packet that should be applied back to the source document.

